<ContentDialog
    x:Class="VipFit.Views.SaveChangesDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:VipFit.Views"
    Title="Zapisaæ zmiany?"
    Content="Masz niezapisane zmiany, które zostan¹ utracone. Czy chcesz zapisaæ zmiany?"
    PrimaryButtonText="Zapisz"
    SecondaryButtonText="Nie zapisuj"
    CloseButtonText="Anuluj"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    SecondaryButtonClick="ContentDialog_SecondaryButtonClick"
    CloseButtonClick="ContentDialog_CloseButtonClick"
    DefaultButton="Primary"
    mc:Ignorable="d">

    <ContentDialog.Resources>
        <Style TargetType="local:SaveChangesDialog" BasedOn="{StaticResource DefaultContentDialogStyle}"/>
    </ContentDialog.Resources>

</ContentDialog>
